{
  "name": "chatalytics",
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "node": "^6.9",
    "yarn": "^0.21"
  },
  "scripts": {
    "postinstall": "cd frontend && yarn install && yarn build"
  }
}
